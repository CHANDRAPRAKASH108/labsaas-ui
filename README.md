# LabSaaS Web

Next.js UI for LabSaaS. Talks only to the API (no Prisma).

## Setup

```bash
cd labsaas-web
cp .env.example .env   # set API_URL=http://localhost:4000
npm install
npm run dev
```

- UI: http://localhost:3000  

Sibling API repo: `../labsaas-api`
