import { ComingSoonPage } from "@/components/coming-soon-page";

export default function PackagesPage() {
  return (
    <ComingSoonPage
      title="Packages"
      description="Group tests into sellable packages with bundled pricing. Add packages from the master catalog or create custom bundles for this lab."
    />
  );
}
