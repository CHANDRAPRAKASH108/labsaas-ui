import { ComingSoonPage } from "@/components/coming-soon-page";

export default function Page() {
  return (
    <ComingSoonPage
      title="Doctors"
      description="Manage referring doctors (name, specialization, contact, registration, clinic). Doctors link to test orders — not directly to patients."
    />
  );
}
